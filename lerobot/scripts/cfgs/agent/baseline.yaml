# @package agent
_target_: agent.baseline.BaselineAgent
obs_shape: ??? # to be specified later
action_shape: ??? # to be specified later
dataset_statistics: ??? # to be specified later
device: ${device}
use_tb: ${use_tb}
stddev_schedule: 0.1
stddev_clip: 0.3
# augment: True # DEFAULT
suite_name: ${suite.name}
obs_type: ${obs_type}
# use_contact_features: ${use_contact_features}
enable_arm: ${eval}
enable_camera: ${eval}
name: baseline
load_bc: False